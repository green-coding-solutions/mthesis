name: Rust Benchmark (Fasta)
author: André Brandão <https://github.com/brandao07>
description: Rust benchmarks using Rust 1.93 Alpine

services:
  rust-container:
    image: rust:1.93-alpine
    setup-commands:
      - command: sh -lc "apk add --no-cache rust cargo"
      - command: sh -lc "mkdir -p /tmp/rust-prefetch/fasta && cp /tmp/repo/benchmarks/rust/fasta/Cargo.toml /tmp/rust-prefetch/fasta/Cargo.toml && CARGO_HOME=/tmp/cargo-home cargo fetch --manifest-path /tmp/rust-prefetch/fasta/Cargo.toml"
    command: sleep infinity

flow:
  - name: Rust-Fasta
    container: rust-container
    commands:
      - type: console
        command: sh /tmp/repo/benchmarks/rust/fasta/build_in_tmp.sh
      - type: console
        command: /tmp/rust-fasta 100
